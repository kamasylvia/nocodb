# r6-f07-lane4 — F07 前端第 6 轮收敛确认(第 4 路:int + rev)

日期:2026-09-12。对象:`packages/nc-gui/components/dashboard/settings/base/Snapshots.vue` + dev 环境 UI 全链路。

## 裁决:PASS

## int(UI 浏览器实测,camoufox-cli)

环境:前端 :3000(Nuxt dev)/ 后端 :8080(rspack dev,nocodb-dev 库)。base 经 API 预建(`POST /api/v2/meta/bases`,资源前缀 `f07r6c_`,id `pgu7b9vbsz6wtbm`)。

| 步骤 | 结果 |
|---|---|
| UI 登录(f01e2e@ce-ee.local) | ✔(signin 表单真实输入+点击) |
| 进入 Manage Snapshots | ✔(`/?page=snapshots` 路由模式;`.nc-base-snapshots` 渲染,`base-snapshots-create` 可见) |
| UI New Snapshot 点击 | ✔(eval + elementFromPoint 合成 MouseEvent 全链 pointer/mouse 事件) |
| 状态收敛 COMPLETED | ✔(UI 行 `base-snapshots-row-snapx5hkrlg9nllpes` 状态 `completed`;UI 内部 2.5s 轮询工作正常) |
| UI Restore 点击 | ✔(`base-snapshots-restore-*` 非 disabled) |
| Restore 断言 | ✔ 跳转 `http://localhost:3000/nc/p198lgi85ujac9g`,页面渲染 `F07r6c_base (Restored)`(UI capitalize 样式,实际 title `f07r6c_base (restored)`);success toast 与 navigateTo 同一 try 分支,跳转达成即 success 路径已执行 |
| API 交叉验证 | ✔ GET base `p198lgi85ujac9g`:title `f07r6c_base (restored)`、deleted=false;2 条 snapshot 记录均 `completed` 且各有 `snapshot_base_id` |
| 清理 | ✔ DELETE snapshots ×2(200)、restored base(200)、测试 base(200);base 列表复核 f07r6c 残留 = NONE |

### 环境观察项(非 F07 违反)

- **共享 dev 后端间歇重启导致 UI 会话反复失效**:rspack dev 每次重启后 `NC_AUTH_JWT_SECRET` 重生成,旧 JWT 全部 401,前端拦截器清 token 弹回 signin。实测复现 ≥3 次(ps 确认 rspack 进程 12:09AM 重启,重启后旧 token curl 401、新 token 200)。这是多 lane 共享 dev server 的环境问题,**非 F07 代码缺陷**;F07 相关 API(POST/GET/DELETE snapshots、restore)在 token 有效期内全部行为正确。另:第一次 create 点击后 UI 被环境踢下线,但后端 job 照常完成(snapshot 16:58 落库),异步 job 的健壮性得到反证验证。
- 纯 UI 表单登录与 token 注入两条路径均已验证可达快照页;`?page=snapshots`(query 模式)渲染正常,`/settings/snapshots`(path 模式)页面骨架在但 tab 内容不挂——两者均为上游 View.vue 布局分支,fork 未改动该区域,且 query 模式为 BaseSettingsMenu 实际导航产物,不影响功能。不判违反,供 orchestrator 参考。

## rev(代码终审 + 测试)

- `Snapshots.vue` 全文终审:API 路径与后端 controller 一致(`/api/v2/meta/bases/:baseId/snapshots[/:id][/restore]`);async 创建 + 按 id 轮询(24×2.5s 上限)状态收敛正常(实测);restore 取 `res.data.base_id` 后 `navigateTo(/nc/<id>)`(实测跳转正确);delete 走 Modal.confirm + 列表刷新(API DELETE 实测 200);restore 按钮对非 completed 禁用;i18n 全走 `t()`;data-testid 齐全;错误路径统一 `extractSdkResponseErrorMsg` + error toast。
- vitest 实跑:`cd packages/nc-gui && npx vitest run test/base-variables-acl.test.ts test/unique-constraint-helpers.test.ts` → **2 files, 10/10 passed**(2+8)。

### 找茬

无(无 error 级问题)。两条非违反备注,不计 issues:

1. `createSnapshot` 的 success toast 在 POST 受理即弹(早于 status=completed);UI 已有按 id 轮询补偿状态显示,fork 设计取舍,实测行为可接受。
2. 组件卸载后轮询循环不中止(无 AbortController/卸载标志),最坏情况设置弹窗关闭后 60s 内多余 toast;功能无影响,轻微。

## 结论

int PASS(全链路:登录 → Manage Snapshots → New Snapshot → COMPLETED → Restore → 跳转断言,API 交叉验证一致,资源已清理)+ rev PASS(终审无违反,测试 10/10)→ **总裁决 PASS**。
