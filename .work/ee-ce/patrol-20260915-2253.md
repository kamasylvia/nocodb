# patrol-20260915-2253 — 整点巡检（22:53 触发，窗口判定：18–23 外部→已被临时覆盖为 5 subagents）

## 窗口判定

22:53 属 18:00–23:00 窗；按 22:50 用户临时覆盖指令，阵容 = 5 路 ZCode subagents（外部 CLI 停用至用户重配）。

## 环境自愈（大动作，详见事故记录）

- 前端 :3000 宕 → 已拉起（pnpm dev）。
- 后端 :8080 宕 >100min → 深度排障定因：
  1. 直接原因：外部阵容（omp 路）kill 后端 + 多路自愈互相 pkill（kill 战争），已全部清场。
  2. **底层根因**：repo 在外置 HDD（/Volumes/UNITEK，24MB/s 顺序、单头随机读 ~8-10ms/文件），rspack bundle 外部化的 node_modules 数十万小文件。文件缓存冷后（今天多次大作业挤出），node 冷启动随机读 = 每秒 ~10-35 模块，全部加载需小时级；表现为主.js 零输出、不绑端口、进程 UN 态。
- **处置**：放弃 UNITEK 上的启动，整仓 tar 拷贝到内置 SSD（~/.nocodb-run，46Gi 空闲，拷贝中预计 40-70min 一次性代价），完成后从内盘启动后端（秒级加载）。期间五路只轮询 8080，不抢盘 I/O。
- 经验：外置盘 repo 的后端冷启动在缓存被挤出后不可用； patrol 自愈不得在盘 I/O 饱和时叠加随机读任务（第一次 find 预热 sweep 与 node 互踩，速率反降 5 倍）。

## 在飞路巡检

- lane5：代码复审完成（0 error）+ 质量门双过（tsc 0 / jest 26/26）；测试脚本就绪，轮询等 8080。主动汇报纪律好。
- lane1-4：22:50 派遣，复审阶段，正常。
- 无报告落盘 → 不裁决。

## 待办

- tar 拷贝完成 → 从 ~/.nocodb-run 启动后端 → health 200 → 五路自动开测。
- GOAL-STATE 后端状态行更新（内盘副本方案）。

## 功能状态三分表

| 状态 | 功能 |
|---|---|
| 已完成(pass) | F05（6ab23da0）、F01（744d3161）、F07（bc409929da）、F10（ab31f60fe3）、F08（e737f8f3ec）、F02（ca6c81f5a6） |
| 进行中 | F03 Data permissions — R4 五路 subagents 在飞（lane5 复审+质量门已完成，全路等后端恢复开集成测试）；连击 0/3 |
| 未做 | F04 Manage Syncs、F06 Docs Permissions、F09 Sync data |
